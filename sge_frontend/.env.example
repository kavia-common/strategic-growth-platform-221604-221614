# Frontend environment variables for SGE (Create React App)
# Copy this file to .env and set the values accordingly.

# Supabase project URL (found in your Supabase project settings)
REACT_APP_SUPABASE_URL=https://YOUR-PROJECT-REF.supabase.co

# Supabase anonymous public key (found in your Supabase project API settings)
REACT_APP_SUPABASE_ANON_KEY=YOUR-ANON-KEY

# Backend API base URL for Axios (sge_backend). Include protocol and port.
# Example for local dev using port 3001:
REACT_APP_API_URL=http://localhost:3001

# Public site URL used for Supabase email redirect flows (optional but recommended).
# This should be the deployed site URL in production, or http://localhost:3000 for dev.
REACT_APP_SITE_URL=http://localhost:3000
